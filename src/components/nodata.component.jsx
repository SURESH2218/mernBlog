const NoDataMessage = ({ message }) => {
  return (
    <div className="text-center w-full btn-light p-4 rounded-full bg-grey mt-4">
      <p>{message}</p>
    </div>
  );
};
export default NoDataMessage;
